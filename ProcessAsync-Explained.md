# لایه امنیت آپلود فایل — معماری، مأموریت‌ها و فرآیند `ProcessAsync`

> هدف استراتژیک پروژه در یک جمله:
> **هرگز به حرف فایلی که می‌فرستد اعتماد نکن.** کاربرِ مخرب می‌تواند یک `exe` یا بدافزار را با اسمِ `cat.jpg` بفرستد. این کتابخانه «ادعای» فایل (پسوند، MIME، اندازه) را کنار می‌گذارد و «حقیقتِ» فایل را از روی بایت‌های واقعی و ساختار داخلی‌اش برمی‌دارد — سپس این دو را می‌سنجد. به‌علاوه چهار حمله‌ی کلاسیکِ آپلود (بمب فشرده، فرار از پوشه با `../`، مکروی آفیس، تصاویر بمبی) را هم خنثی می‌کند.

---

## بخش ۱ — نگاه بیزینسی / استراتژیک (چرا وجود دارد؟ چه تهدیدی را خنثی می‌کند؟)

### ۱.۱ اصول راهبردی

| اصل | معنا |
|---|---|
| بی‌اعتمادی به اعلام | قبل از هر چیز فرض کن نام، پسوند، MIME و اندازه همه می‌توانند دروغ باشند |
| راستی‌آزمایی هویت | هویت واقعی فایل از بایت‌ها و ساختار داخلی خوانده می‌شود، نه از لیبل |
| سازگاری ادعا با واقعیت | اگر لیبل با حقیقت یکی نبود → رد یا اخطار طبق پالیسی |
| Fail-Closed (بسته از پیش‌فرض) | هر چیزی که مشخص/مجاز نشده باشد = رد، نه قبول. حتی نبودِ اسکنر بدافزار = رد |
| دفاع در عمق | چهار لایه‌ی مستقل: هویت‌سنجی → سازگاری → ساختار → پالیسی/اسکن |

### ۱.۲ نقشه‌ی لایه‌ها از دید مأموریت

| لایه | کلاس‌ها | مأموریت امنیتی |
|---|---|---|
| هماهنگ‌کننده | `FileUploadPipeline`، `FileUploadPipelineBuilder`، `FileUploadPipelineExtensions` | گیت اصلی؛ ترتیب اجرای ایست‌ها، کنترل تراکم (ضد سیل/DoS)، سیم‌کشی DI به‌صورت Singleton |
| تشخیص (هویت واقعی) | `FileTypeResolver`، `FileSignatureDetector`، `FileSignatures`، `TextFormatDetector`، `MimeDetector`، `ExtensionResolver`، `FileExtensionRegistry` | کشفِ «فایل واقعاً چیست» — مقابله با تقلب نوع فایل و XSS |
| اعتبارسنجی (ایست‌های بازرسی) | `FileNameValidator`، `FileExtensionValidator`، `FileSizeValidator`، `FileSignatureValidator`، `FileContentValidator`، `ImageStructureValidator`+`ImageDimensionParser`، `PdfStructureValidator`، `ArchiveStructureValidator`، `OfficeStructureValidator`، `CompositeValidator` | اعمال قواعد روی نام، پسوند، اندازه، امضا، ساختارِ درون ظرف (بمب زیپ، مکرو، `../`) |
| پالیسی و بدافزار | `FileUploadPolicy` و گروه‌ها، `PolicyEngine`، `IMalwareScanner`+`MalwareScanResult`+`MalwareScanning` | قوانین سازمان + قاضی اجرای آن + درگاه اسکنر واقعی (مثل ClamAV) |
| فایل موقت | `TempFileStream`، `TempFileCleaner` | دفاع در عمق؛ پاک‌سازی فایل‌های موقت مانده پس از کرش (ضد انباشت دیسک) |

### ۱.۳ جدول کامل کلاس‌ها با مأموریت امنیتی (بیان ساده)

**لایه‌ی هماهنگ‌کننده — گیت اصلی امنیت**

| کلاس | مأموریت امنیتی |
|---|---|
| `FileUploadPipeline` | ژنرالِ صحنه؛ همه‌ی ایست‌های بازرسی را به‌ترتیب اجرا می‌کند، ورودی را قبل از هر چیز در یک بافر/فایلِ امن و قابل‌جستجو (seekable) قرار می‌دهد تا تسترها نتوانند با stream بپیچانندش، و اجازه نمی‌دهد هکر با هزاران آپلود هم‌زمان حافظه را قورت دهد (سمافور = کنترل تراکم). |
| `FileUploadPipelineBuilder` | کیت تنظیم گیت؛ مشخص می‌کند کدام validatorها، کدام سرویس تشخیص و کدام اسکنر بدافزار نصب شود. اگر هیچ validator نسازید، به‌طور پیش‌فرض ۹ ایست امن نصب می‌کند. |
| `FileUploadPipelineExtensions` | سیم‌کشی به پروژه‌ی شما؛ pipeline را به‌صورت Singleton در DI ثبت می‌کند تا تکی باشد و تمام درخواست‌ها از یک گیت عبور کنند. |

**لایه‌ی تشخیص — راستی‌آزمایی هویت واقعی فایل** (مقابله با «فایل مخرب با پسوند بی‌خطر»؛ فرض: نام فایل دروغ است)

| کلاس | مأموریت امنیتی |
|---|---|
| `FileTypeResolver` | تشخیص‌دهنده‌ی هویت؛ به فایل می‌گوید «تو واقعاً چی هستی؟». اول فرمت‌های ظرف (container مثل ZIP/RIFF/OLE)، بعد انگشت‌نگاری بایت‌ها، بعد متن. اگر هیچ‌کدام → «ناشناخته» که با سیاست fail-closed رد می‌شود. |
| `FileSignatureDetector` | دستگاه خط‌شناس (انگشت‌نگار)؛ مقایسه‌ی بایت‌های ابتدای فایل با الگوهای شناخته‌شده. یک `jpg` واقعی با `FF D8 FF` شروع می‌شود نه با `MZ`. اینجا جایی است که دروغِ پسوند عریان می‌شود. |
| `FileSignatures` | بانک تصویر مجرمان؛ کاتالوگ تمام امضاهای باینری شناخته‌شده (JPEG، PNG، PDF، EXE، RAR و…) که خط‌شناس با آن مقایسه می‌کند. |
| `TextFormatDetector` | تشخیص فایل‌های متنی بدون امضای باینری (XML، SVG، CSV، JSON). حیاتی برای هک XSS: یک «عکس» که در واقع HTML/JS است شناسایی می‌شود. |
| `MimeDetector` | تست‌کننده‌ی ادعای MIME؛ اگر نوع اعلام‌شده با نوع واقعی در تضاد باشد گزارش می‌دهد. |
| `ExtensionResolver` | کارشناس پسوند؛ پسوند را نرمال و بی‌ضرر می‌کند و تله‌های نام را می‌گیرد: مسیرفرار (`../`)، کدگذاری دوتایی، نال‌بایت، چند پسوند (`file.jpg.exe`). هکرها دقیقاً از همین‌ها برای گذر از فیلترها استفاده می‌کنند. |
| `FileExtensionRegistry` | فهرست مجازِ داخلی؛ برای هر دسته می‌گوید «این پسوندها قانونی‌اند». allowlist از قبل ساخته شده و فراموش‌کردنش غیرممکن است. |

**لایه‌ی اعتبارسنجی — ایست‌های بازرسی مستقل**

| کلاس | مأموریت امنیتی |
|---|---|
| `FileNameValidator` | دفاع از فایل‌سیستم سرور؛ نامِ آپلودشده قرار است روی دیسک شما بنشیند. جلوگیری از مسیر فرار (`../../etc/passwd`)، نام‌های رزرو ویندوز (`CON`، `NUL`)، نال‌بایت، و کاراکترهای مخرب. |
| `FileExtensionValidator` | پسوند باید در allowlistِ دسته‌ی خودش باشد؛ پسوندِ بیرون از دسته‌ی مجاز، حتی اگر جعلی هم نباشد، رد می‌شود. |
| `FileSizeValidator` | دفاع از حافظه/دیسک (DoS)؛ جلوی آپلود فایلِ چندگیگابایتی به اسمِ «عکس» را می‌گیرد و فایلِ خالی را رد می‌کند. |
| `FileSignatureValidator` | گیتِ «ادعا با واقعیت»؛ پسوندِ درخواستی باید با امضای واقعی فایل یکی باشد. اگر `abc.png` باشد ولی بایت‌هایش `MZ` باشد → رد. **قلبِ امنیت راهبردی پروژه.** |
| `FileContentValidator` | سازگاریِ ادعای MIME و اندازه با وضعیت واقعی (مقابله با جعلِ محموله‌ی کوچک ولی اعلامِ بزرگ). |
| `ImageStructureValidator` + `ImageDimensionParser` | ضد «بمب پیکسل»؛ یک فایل ~۱ کیلوبایتی می‌تواند اعلام کند ۶۰,۰۰۰×۶۰,۰۰۰ پیکسل است تا حافظه‌ی رندر قورت داده شود. ابعاد و تعداد پیکسلِ واقعی از ساختار (نه از ادعا) خوانده و با سقف شما مقایسه می‌شود. |
| `PdfStructureValidator` | تثبیت واقعی‌بودن PDF؛ هدر `%PDF-` و آیتم پایانی `%%EOF` را چک می‌کند؛ فایل غیر-PDF تغییرنام‌داده لو می‌رود. |
| `ArchiveStructureValidator` | وزنه‌ی سنگین امنیت؛ چهار تهدید آرشیو: بمب زیپ (نسبت فشرده‌سازی ده‌برابری برای سد کردن دیسک)، تعداد انتری بی‌نهایت، آرشیو تو در توی بی‌نهایت، و کشف `../` در مسیر انتری (استخراج خارج از پوشه‌ی مقصد). |
| `OfficeStructureValidator` | ضد مکروی آفیس؛ `Word/Excel/PowerPoint` با مکرو (`vbaProject`) رایج‌ترین وسیله‌ی نفوذ است. ساختار واقعی سند OOXML و نبودِ مکرو اجباری می‌شود مگر صریحاً مجاز باشد. |
| `CompositeValidator` | بسته‌بندِ چند ایست؛ چند validator را زیر یک نام گروه می‌کند تا فلوهای سفارشی اعتبارسنجی بسازید. |
| `StreamHelper` | تجهیزات مشترک؛ خواندن امنِ پیشوندِ فایل (حتی برای stream غیرقابل‌جستجو) بدون دست‌زدن به موقعیت. زیرساختِ امنِ همه‌ی ایست‌ها. |

**لایه‌ی پالیسی و بدافزار**

| کلاس | مأموریت امنیتی |
|---|---|
| `FileUploadPolicy` و گروه‌ها (`FileKinds`، `FileSizes`، `FileNames`، `Structures`، `MalwareScanning`) | سندِ قوانین امنیتی سازمان؛ چه نوع‌هایی مجاز، حداکثر اندازه، قواعد نام، سقف ساختار، آیا اسکن بدافزار اجباری است. همه‌ی پیش‌فرض‌ها محتاط (fail-closed) هستند: اگر چیزی را مشخص نکنید = رد، نه مجاز. |
| `PolicyEngine` | قاضی پالیسی؛ نوعِ کشف‌شده و اندازه را با سند قوانین می‌سنجد و حکم می‌دهد: مجاز / رد / اخطار. بدون این، قوانین سازمان هرگز اجرا نمی‌شد. |
| `IMalwareScanner`، `MalwareScanResult`، `MalwareScanning` | درگاه اسکنر واقعی ضدبدافزار (مثل ClamAV)؛ اگر سازمان «اسکن اجباری» داشته باشد، هیچ فایلی بدون رأیِ `Clean` رد نمی‌شود؛ اگر اسکنر در دسترس نبود باز هم fail-closed رد می‌شود. |
| `FileUploadSecurityException`، `FileValidationException` | زبان هشدار؛ خطای امنیتی سنگین در برابر خطای اعتبارسنجی معمولی تفکیک می‌شود تا تیم امنیت بداند چه را پیگیری کند. |

**لایه‌ی فایل موقت — دفاع در عمق**

| کلاس | مأموریت امنیتی |
|---|---|
| `TempFileStream`، `TempFileCleaner` | ضد انباشتِ فایل‌های موقتِ مانده؛ فایل‌های موقتی که سرور روی دیسک می‌نویسد اگر بعد از کرش پاک نشوند دیسک را پر می‌کنند (DoS). با `DeleteOnClose` پاک می‌شود و تمیزکننده‌ی پشتیبان فقط فایل‌های هم‌نامِ دقیقِ خودِ کتابخانه و با سن بالا را حذف می‌کند — به فایل‌های موقتِ دیگر برنامه‌ها دست نمی‌زند. |

### ۱.۴ چکیده‌ی بیزینسی

چهار لایه‌ی حفاظت:
1. **هویت‌سنجی** — فایل واقعاً چیست؟
2. **سازگاری** — ادعای کاربر با واقعیت یکی باشد.
3. **ساختار** — درون ظرف تله نباشد (بمب زیپ، مکرو، مسیر فرار).
4. **پالیسی** — قوانین سازمان + اسکن بدافزار.

---

## بخش ۲ — نگاه فنی / پیاده‌سازی (فرآیند داخلی `ProcessAsync`)

### ۲.۱ امضای متد

```
Task<FileValidationResult> ProcessAsync(FileUploadRequest request,
                                         CancellationToken cancellationToken = default)
```

ورودی `FileUploadRequest`: `FileStream` (الزامی) • `OriginalFileName` (الزامی) • `DeclaredMimeType?` • `DeclaredFileSize?` • `Policy?` • `CancellationToken`
خروجی `FileValidationResult`: `IsValid` • `DetectedFileType` • `Errors` • `Warnings` • `FileSizeBytes` • `MalwareScanResult` • `ValidatedAt` • `ValidationDuration` • `Metadata`

### ۲.۲ گام‌به‌گام فرآیند

#### گام ۰ — سر در ورودی (قبل از هر چیز)
- خود `request` و `request.FileStream` نباید `null` باشند (اگر `null` → استثنا).
- **پالیسی اجباری است**: اگر `request.Policy` تنظیم نشده باشد → `InvalidOperationException`. بدون پالیسی = توقف کل. پیاده‌سازیِ fail-closed: پالیسی فقط می‌تواند صریح باشد.
- **انتخاب توکن لغو**: اگر توکنِ پاس‌داده‌شده به متد قابلیت کانسل دارد همان استفاده می‌شود، وگرنه `request.CancellationToken`.

#### گام ۱ — کرکره‌ی ظرفیت (کنترل تراکم)
دسترسی با دو سمافور محدود می‌شود:
- سمافور **هم‌زمانی** (`maxConcurrentUploads = 10`): فقط ۱۰ آپلود در آنِ واحد.
- سمافور **اعتبارسنجی** (`32 هم‌زمان + 32 صف`): اعتبارسنجی سنگین‌تر است، سقف جدا دارد.

**چرا؟** بدون این، هزاران درخواست هم‌زمان پیپلاین را در حافظه و CPU قورت می‌دهد = DoS. کنترل تراکم بخشی از اعتبارسنجی است.

#### گام ۲ — تبدیل به stream امن (تضمین seekable)
`EnsureSeekableAsync`:
- اگر `CanSeek` → همان stream، بدون مسئولیت dispose.
- اگر نه → `BufferToTempFileAsync`:
  - **فایل موقت** (`{Guid:N}.tmp` با `FileOptions.DeleteOnClose`، `FileShare.None` = هیچ‌کس دیگری نمی‌تواند هم‌زمان به آن دست بزند).
  - **سقف خواندن** `MaxFileSizeBytes + 1` اعمال می‌شود؛ منبعِ بی‌پایان برای همیشه خوانده نمی‌شود.
  - بافر از `ArrayPool` رنت می‌شود (چانک ۶۴KB) و به‌صورت تضمینی برمی‌گردد.
  - در هر خطا: فایل موقت dispose و `File.Delete` می‌شود، سپس خطا بالا می‌رود.

**چرا؟** همه‌ی ایست‌های بعدی (تشخیص/validatorها) نیاز به «بازگشت به ابتدا» و «خواندن دوباره» دارند. stream یک‌جهته (شبکه/پایپ) را نمی‌توان دوباره خواند → اول کپی امن، و حرفِ امنیتی: نسخه‌ی موقت روی دیسک است با سقف.

#### گام ۳ — هویت‌یابی (تشخیص نوع واقعی)
- `stream.Position = 0` و سپس `DetectAsync` با دو ادعای کاربر: پسوندِ نرمال‌شده‌ی نام فایل + MIME اعلامی.
- خروجی `detectedType` شامل: پسوند واقعی، MIME، دسته، فرمت، و `ExtensionMatchesSignature` (آیا پسوندِ اعلامی با امضا یکی است؟).

#### گام ۴ — رژه‌ی validatorها (جمع‌کردن خطا، بدون توقف)
- قبل و بعد هر validator: `ThrowIfCancellationRequested`.
- **همه‌ی ایست‌ها اجرا می‌شوند**؛ هیچ‌کدام با شکستِ دیگری متوقف نمی‌شود؛ خطاها در یک لیست جمع می‌شود (`errors.AddRange`).

**چرا؟** (۱) نمی‌گذاریم فایلی که در گیت ۱ رد شد، گیت ۳ را دور بزند — بیشترین اطلاعاتِ گزارش؛ (۲) رفتار تنبیه‌ی‌نشدنیِ یکنواخت برای pipeline.

#### گام ۵ — اندازه‌گیری و حکم پالیسی
- `GetSize`: اگر seekable → `Length`، وگرنه `Position` (اندازه‌ی حقیقی).
- `PolicyEngine.Evaluate(detectedType, fileSize, policy, ct)` → خطاها + هشدارها. قاضی: آیا دسته مجاز است؟ پسوند در allowlist دسته است؟ MIME مجاز؟ اندازه در بازه؟ mismatch رد یا هشدار؟

#### گام ۶ — جمع‌بندی وضعیت
- بدون خطا → `Success(detectedType, fileSize)`؛ با خطا → `Failure(errors)`.
- هشدارهای پالیسی با `AddWarning` افزوده می‌شوند — هشدارها فایل را رد نمی‌کنند، فقط اطلاع می‌دهند.

#### گام ۷ — اسکن بدافزار (اختیاری ولی fail-closed)
- فقط اگر نتیجه هنوز `IsValid` **و** پالیسی `RequireMalwareScan`.
- ماشین وضعیت `RunMalwareScanAsync`:
  - بدون اسکنر و `RejectIfMalwareScanUnavailable=true` → خطای `MalwareScanRequired` (fail-closed).
  - `Clean` → قبول. `Infected` → خطای `MalwareScanInfected` (با نام تهدید).
  - `Error` → طبق `MalwareScanErrorPolicy`؛ `Unknown/NotSupported` → طبق `MalwareScanUnknownPolicy`. همه‌ی پیش‌فرض‌ها `Reject`.
  - وضعیتِ نهایی (`scanRecord.Status`) در `MalwareScanResult` نتیجه ثبت می‌شود.

#### گام ۸ — بستن پرونده (پاک‌سازی تضمینی)
- `ValidationDuration = stopwatch.Elapsed`.
- **finally مرتب**: سمافور اعتبارسنجی رها؛ اگر ما stream را ساخته بودیم (فایل موقت) → dispose (که `DeleteOnClose` حذف دیسک را انجام می‌دهد)؛ سپس سمافور هم‌زمانی رها.
- **catch `OperationCanceledException`**: چون رهاسازیِ عادی اجرا نشده، سمافورها در `try/catch` رها می‌شوند (مقاوم در برابر رها کردنِ سمافوری که هرگز گرفته نشده → جلوگیری از `SemaphoreFullException`) و بعد استثنا دوباره propagate می‌شود.

### ۲.۳ نقشه‌ی یک‌خطی

```
ورودی → کنترل null / پالیسی / توکن
      → سمافور ظرفیت (ضد سیل)
      → seekable? (مستقیم، یا کپی امن به فایل موقت با سقف)
      → DetectAsync (هویت واقعی)
      → همه‌ی validatorها (جمع خطا، بدون توقف)
      → GetSize + PolicyEngine (قاضی)
      → ساخت نتیجه (Success / Failure + هشدارها)
      → اسکن بدافزار (فقط اگر IsValid و درخواستی؛ fail-closed)
      → ValidationDuration + پاک‌سازی سمافور / فایل موقت
```

### ۲.۴ لحظه‌های کلیدیِ تمایز

1. **پالیسی اجباری** — پیش‌فرضِ بسته است، نه «سست تا رد نشود».
2. **مدیریت تراکم و حافظه** — سمافورها + فایل موقتِ با سقف + سطل `ArrayPool` + سقفِ خواندن.
3. **اسکنر fail-closed** — نبودِ اسکنر هم یعنی رد، نه قبول.